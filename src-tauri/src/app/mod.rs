//! Desktop shell concerns that belong to no single feature.
//!
//! `background` is the tray and run-at-login behaviour; `email` sends the
//! monitor notifications.

pub mod background;
pub mod email;
